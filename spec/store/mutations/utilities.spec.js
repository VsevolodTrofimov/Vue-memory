import * as mutations from '@/src/store/mutations.js'

describe('Mutations', () => {
  describe('Utilities', () => {
    const state = {}

    beforeEach(() => {
      state.timeouts = {}
    })

    describe('delayMutation', () => {
      it('Sets timeout', () => {

      })

      it('Overwrites old timeout', () => {
        
      })
    })
  })
})